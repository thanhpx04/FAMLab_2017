/*
 * SVD.cpp
 *
 *  Created on: Feb 2, 2017
 *      Author: linh
 */

#include "SVD.h"

/*
void svdMethod(Matrix<int> m, Matrix<float> &u, Matrix<float> &s,
	Matrix<float> &v)
{
	int rows = m.getRows(); // normally, size of m is 2x2
	int cols = m.getCols();
	if (rows == cols == 2)
	{
		int m00 = m.getAtPosition(0, 0);
		int m01 = m.getAtPosition(0, 1);
		int m10 = m.getAtPosition(1, 0);
		int m11 = m.getAtPosition(1, 1);

	}
}
*/
